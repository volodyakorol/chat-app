import React from 'react';

import { TFileType } from '@/shared/types/file.types';

type PropsT = {
  types: TFileType[];
};

export const Files = ({ types }: PropsT) => {

  return <></>;
};
